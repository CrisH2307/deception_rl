# T7: Arm B model runs and analysis (Wave 2)

Execute the task below. Obey `CLAUDE.md` invariants. If any instruction here
conflicts with `CLAUDE.md`, stop and report the conflict rather than choosing.

---

You are running the language-model arm of Paper 2.

PRECONDITION
T6's kill gate must have passed. Confirm this before spending any inference budget.
If you cannot confirm it, stop.

INFERENCE TARGET
Kaggle T4s, matching Paper 1's environment. Chosen to maximise the chance `F0`
reproduces Paper 1's frozen `cond4`, which is also what calibrates P2-D13's
inertness floor, so environment match is not only a replication concern.

SCOPE
All 1,000 items, both permutations, three framings, seven models. The `size`-tile
108 remains the confirmatory set (P2-D3, P2-D4). The full set is needed because
step 4's `beta_c = infinity` control runs on the 540 non-divergent items and the
`F0` replication is stronger on the full set. Scoring is one teacher-forced
forward pass per option with no sampling, so the full set is affordable.

INPUTS
- Prompt templates F0/F1/F2 from T3
- Frozen item set from T2
- Oracle values (o*_0, o*_infinity, divergence set membership, beta_c) from T1/T6
- Paper 1's model ladder and inference harness at `$P1_ROOT/src/score_llm.py`.
  Reuse it. Same models, same decoding settings, same parsing, same seeds
  convention. `$P1_ROOT` defaults to the path recorded in
  `docs/spec/adversary-game-v1.md` and is resolved by `src/p1.py`. Note that the
  harness does not sample: the chosen option is the argmax over teacher-forced
  option log-probabilities, so there is no decoding temperature to match.

TASK
1. Run the ladder across all three framings. Record raw outputs; never overwrite them.
2. F0 sanity check FIRST: does F0 reproduce Paper 1's reported result on the overlapping
   items? Report the comparison before analyzing F1/F2.

   **Corrected 2026-09-10 by the author. The earlier wording, "if not, everything
   downstream is suspect", was too strong and is withdrawn.** The F0 re-run serves
   two separable purposes and they fail separately:

   - **Environment equivalence** against Paper 1's frozen `cond4`. If this fails on
     floating-point or environment grounds, the link to Paper 1 weakens: the
     comparison to Paper 1's published reference set (step 5) is then approximate
     and must be reported as such.
   - **A within-environment baseline** for the F1 and F2 contrasts. This does not
     depend on the first. If equivalence fails, F0 is still the baseline the
     contrasts are taken against, they are still internally valid, and Arm B's
     result stands on its own environment.

   Report both, separately, with the consequence of each stated. Do not report a
   failure of the first as invalidating the second.

   **Do NOT substitute Paper 1's frozen `cond4` for F0.** F1 and F2 must run in the
   same environment as their own baseline, or the contrast mixes framing with
   hardware. Re-run F0 even when the frozen rows look like they would serve.
3. Primary analysis, restricted to the divergence set: signed movement of the choice
   distribution from o*_0 toward o*_infinity, F1 minus F0 and F2 minus F0, per model,
   with the preregistered uncertainty treatment.
4. Control analysis on the beta_c = infinity items: there should be NO movement there,
   because the optimal signal does not change. Movement there is a prompt-sensitivity
   artifact, not adversary awareness, and it caps how much of the divergence-set effect
   you can attribute. Report it as such.
5. Place every model against the full reference set from Paper 1 (salience, marginal
   null, Bayes) plus the new adversary oracle. The point is where models sit on the
   interval, not whether they beat a baseline.
6. Size-ladder analysis: does adversary sensitivity appear at any scale?

ANTI-REQUIREMENTS
- Do not retry, resample, or filter outputs to improve parse rates beyond the
  preregistered parsing rule. Report parse failures as data.
- Do not report win rates or accuracy as the headline. The headline is position
  relative to the references.
- If a model's behaviour is indistinguishable from the marginal null, say so plainly.

DELIVERABLE
Raw outputs, results file, figures, and an explicit statement of the F0 replication
result and the beta_c = infinity control result.

---

## Note added 2026-09-09 by the T6 session. Two things T7 must not rediscover.

### 1. BINDING: `A`'s domain comes from `beta_c`, never from the sign of `ext_i`

v2.0 section 3.2 defines the domain by an equivalence:

> `ext_i > 0` if and only if `o*_infinity != o*_0`, which is exactly
> `beta_c(i) < infinity`. So `A` is defined precisely on the divergence set.

**The equivalence is true mathematically and false in float64.** On P1's frozen
1,000 it fails on exactly one item. A rival beats `o*_0`'s margin by less than
P1's D51 tie-break tolerance, so the tie-break keeps `o*_0` and `beta_c` is
`infinity`, while `1 - marg_norm(o*_0)` evaluates to `2.2e-16` rather than `0`.

Consequences, with the numbers, so this brief is self-contained:

- An implementation that takes `A`'s domain from `ext_i > 0` computes
  `A = (marg_norm(o) - marg_norm(o*_0)) / 2.2e-16` for an **adversary-robust**
  item, yielding a finite `A` of order `1e16`.
- **Section 6's `ext_i >= 0.02` floor does not catch it if the floor is applied
  after `A` is formed**, which is the natural order to write it in. `2.2e-16` is
  below the floor, so a floor applied first does catch it, but an implementation
  that forms `A` and then filters does not.
- A range check does not catch it either: `A` is legitimately signed and
  legitimately large-negative off the poles (see item 2 below), so `1e16` does not
  stand out from a distribution whose off-pole median is `-3.3`.
- The smallest genuine `ext_i` on the divergence set is `2.09e-04`, four orders of
  magnitude above `2.2e-16`, so the two cases separate cleanly. There is no
  tolerance to tune and none should be introduced.

**Take the domain from `np.isfinite(beta_c)`.** `src/t6_f0_headroom.py` does this
and its `--demo` asserts `A(o*_0) = 0` and `A(o*_infinity) = 1` on all 460
divergent items and `A` undefined on the other 540. Reuse that function rather
than writing a second one; `CLAUDE.md` treats a copied frozen computation as a
duplication bug.

### 2. `A`'s geometry, so a real result is not read as a bug and a bug is not read as a result

Measured on the frozen divergence set, `reports/T6_F0_headroom.md`:

- `A` is **near-bimodal**. Only the two poles carry `A` in `[0, 1]`: `A = 0` at
  `o*_0` on 470 option-cells, `A = 1` at `o*_infinity` on 439. The median over the
  841 off-pole option-cells is **-3.329**.
- This is because `A` divides by `ext_i`, whose median on the divergence set is
  `0.2313`. An option well below `o*_0` on `marg_norm` is arithmetically far
  negative. **Large negative `A` is normal and is not an error.**
- Pearson correlation between P1's salience-Bayes coordinate and `A` at the option
  level is `0.0061`; Spearman is `0.4777`. The two coordinates order options very
  differently, so a result on one does not transfer to the other.

**Do not report negative mean `A` as a finding.** F0 medians are negative for
every model on the ladder, and T6 tested whether anything survives once P1's own
far-side finding is accounted for: leave-one-item-out, predicting `A` from the
choice's position on P1's coordinate, the median residual is exactly `0` for all
seven models **and** for a uniform-random chooser, with no model skewing negative.
Negative `A` is a corollary of Paper 1 re-expressed on the margin coordinate, not
independent evidence that models are worse than the no-adversary optimum.

**Use medians, and report the value from the means beside them, as v2.0 section
3.2 requires.** A mean of per-item `A` is dominated by the off-pole tail. This is
the same ratio pathology P1 measured in `src/frontier_position.py`, and it bites
harder here than on P1's coordinate.

### 3. F0 baseline rates, already measured, for the replication check

`results/T6_F0_headroom.json` carries the F0 numbers per model on the divergence
set from P1's frozen `cond4`, so T7's F0 arm has a target to reproduce rather than
only a design. Per rendering, the share landing on `o*_0` runs `0.2739` to
`0.4228` against a chance rate of `0.2732` (mean of `1/|O|`); the share landing on
`o*_infinity` runs `0.0946` to `0.3239`, below chance for five of the seven. A
point mass at `A = 0` is therefore **not** evidence of Bayes-optimal behaviour,
which is exactly why section 3.3 puts every claim on excess over the marginal
null. `CTRL` is cross-family: per P1's D111 only choice-based rates are comparable
for it, never magnitudes.

---

## Note added 2026-09-10 by the T5.6 session. Three rulings T7 must not re-open.

`PREREGISTRATION_v2.6.md` settles three Arm B questions that were open or
contradictory across `v2.4` and `v2.5`. All three are bound in
`docs/P2/DECISIONS.md` and `src/p2_decisions.py`; a run that disagrees fails at
import. Call `p2_decisions.bind_armb_rates(primary_rate, sign_denominator,
c5_is_active)` from the Arm B analysis script, alongside the `bind_armb`,
`bind_armb_statistic` and `bind_armb_tie` calls already required.

0a. **The inertness floor is NUMERICAL, and T7 sets it (P2-D13).** The floor is
   provisionally **7 items** and is revised to T7's own `F0`-versus-`cond4`
   disagreement count if that count is larger. Call
   `armb_floor.floor_for_run(f0_disagreement_items)` and
   `p2_decisions.bind_armb_floor(...)`. The revision is a maximum, so a quiet
   environment cannot lower the bar. Do not describe 7 as a significance
   threshold: under P2-D9's deterministic scorer the strict null has no sampling
   variation, Type I error is exactly 0, and the bootstrap guards nothing in
   quantity (a). It is retained unchanged in quantity (b).

   Your `F0` disagreement count is the first empirical measurement of the
   no-effect rate `v2.6` section 1.1 could only argue from Paper 1's code path.
   Report it as such.

0b. **A null on (c) is not a null against chance (P2-D14, as corrected by P2-D21
   and P2-D22).** `p0 = 0.5` stands, on the structural ground: moving it
   recalibrates a preregistered test against a different manipulation, on a
   coordinate Paper 1 never used. Do not justify `p0` by calling it conservative;
   conservativeness is a per-model property and it does not hold on `B2`.

   The figures below supersede this step's earlier pair-unit list. At P2-D20's
   item unit, which is the unit (c) runs on, **the content-neutral baseline does
   not drift toward the salience pole.** The per-model proportion runs 0.1951 to
   0.5556. Only `CTRL` resolves at the corrected `alpha` and it resolves
   downward. `B2` sits at the null: 0.5556 on `n_eff` 36, `p` = 0.6177, 95% exact
   [0.3810, 0.7206], and exactly 0.5000 under three of five aggregations of the
   same rows. Do not describe `B2` as drifting upward.

   Report the **signed** per-model gap with every (c) verdict: `CTRL` +0.3049,
   `L3` +0.2576, `L4` +0.1977, `L1` +0.0926, `B4` +0.0397, `L2` +0.0161, `B2`
   **-0.0556**. A positive gap is a Type II cost of that size. `B2`'s is
   negative, which is not a smaller cost but a Type I exposure: a `B2` result
   significant against `p0 = 0.5` but at or below 0.5556 is nominally positive
   while sitting at or below what a content-neutral insert does. Name that
   exposure beside `B2`'s verdict. Do not write "chance".

0c. **The control's sign proportion is admissible (P2-D15).** `sign(ΔA)` depends
   only on which options were chosen and on frozen model-free item geometry, so
   D111 permits it. `logp_sum_chosen`, `logp_neutral_chosen` and any PMI value
   stay inadmissible across families.

0. **The `c5` comparison GATES NOTHING (P2-D12).** Arm B reports three quantities
   and none gates another: (a) inertness, the same-option change rate against
   **zero**, per model, at full `n`, which is where H-B's no-movement half
   resolves; (b) magnitude context, the same-option rate against `R_m`, reported
   with the bootstrap half-width and spending no `alpha`; (c) direction, the sign
   test on `ΔA != 0` against `p0 = 0.5`, unchanged. Zero is the exact null because
   the harness does not sample. The inertness floor is **7 of 108 items** carrying
   a changed rendering, from `results/T5_inertness_ceiling.json`. `v2.5` section
   2.3's gate and `v2.6` section 1.4's outcome table are superseded; read
   `PREREGISTRATION_v2.7.md`, not `v2.6` section 3, for the ceiling. Call
   `p2_decisions.bind_armb_quantities(...)`.

   Do not read the restructure as a power gain. The sign test's effective `n` is
   set by the observed tie rate either way. What changes is that a cell whose
   framing moves about as much as `c5` now reports a sign test with its realized
   power, where the gated design reported it inconclusive and discarded the
   result.

1. **The `c5` reference is an ACTIVE comparator (P2-D9).** It changes the chosen
   option on 0.1389 to 0.3704 of confirmatory pairs, every interval excluding a
   no-effect rate of exactly 1.0, on all seven models. Do not write "F1 moved
   nothing" for an indistinguishable cell. The permitted sentence is "F1 moved
   choices about as much as a known-effective content insertion in the same slot
   did", and it is still not support for H-B. The restated outcome table is
   `v2.6` section 1.4; `v2.5` section 2.5 is superseded, not edited.
2. **The SAME-OPTION rate is primary, not the tie rate (P2-D10).** `v2.4` section
   2.4's label is superseded. The tie rate is the sign test's denominator and is
   reported beside the same-option rate. Report the gap per cell: it is
   non-negative and it is the movement `A` cannot see.
3. **Every reported null carries the joint detection statement (P2-D11).** It is
   computed before T7 runs, into `results/T5_detection_ceiling.json`. Report it;
   do not derive a ceiling from the observed tie rate, which would be a post-hoc
   power calculation.

The reference values `R_m` still come from `v2.5` section 2.2 and are not
recomputed. `src/c5_effect.py --demo` asserts that a recomputation reproduces
them, which is the check that the two documents describe the same artifact.
