# T6: Arm A analysis (Wave 2, RUN FIRST, contains the kill gate)

Execute the task below. Obey `CLAUDE.md` invariants. If any instruction here
conflicts with `CLAUDE.md`, stop and report the conflict rather than choosing.

---

You are running the normative arm of Paper 2. No language models are involved. This
arm cannot fail to produce a result, and it gates the other two arms.

INPUTS
- Spec: <<REPO>>/docs/spec/adversary-game-v1.md
- Oracle implementation from T1
- The UNSTRATIFIED 200,000-candidate pool from Paper 1, for Step 1 only
- **Steps 2 through 5 base: Paper 1's frozen `items_final.parquet`, all 1,000
  items, all four tiles.** This is not a choice made in the running session. It is
  the composition of two records: `PREREGISTRATION_v2.1.md` section 1.3 fixes the
  base as "T2's frozen item set" ("Arm A is T6 Steps 2 through 5 on T2's frozen
  item set"), and `PREREGISTRATION_v2.2.md` section 1.1 fixes what that resolves
  to now that K2 has fired ("The pre-specified fallback is reuse of Paper 1's
  frozen 1,000-item set, `items_final.parquet`"). v2.0 section 2's "Kill-gate item
  base" paragraph says the same thing from the other side: the pool is named as
  the base for the K1 gate *by exception*, "not over T2's frozen item set," which
  presupposes the frozen set is the base for everything else in Arm A.
  Restriction to the `size` tile is Arm B's (`PREREGISTRATION_v2.3.md` section 3)
  and does not reach Arm A.
- **Manifest: Paper 1's, for `items_final.parquet`.** No P2 item file exists, so
  there is no P2 manifest. Chain of custody is the sha256 of P1's frozen artifacts
  against the spec version, as `src/k2_gate.py` already emits.
- Preregistration from T5 (docs/P2/PREREGISTRATION_v2.md), including the kill
  criteria. Section 7.1 is authoritative for Step 1 and this brief restates it;
  if the two disagree, the preregistration wins and this brief is wrong.

TASK, IN THIS ORDER

STEP 1, THE KILL GATE.

Compute the gate over the FULL UNSTRATIFIED 200,000-CANDIDATE POOL, not over T2's
frozen item set.

Why, because getting this wrong invalidates the gate silently: T2 draws its item
set by stratifying ON beta_c, with an explicit quota for the beta_c = infinity
control stratum. A divergence rate measured on that set is therefore a readout of
T2's quotas, not of the signal space. Stratify to oversample divergent items and
the rate goes up; widen the robust quota and it goes down. Neither movement says
anything about whether an informed adversary reorders the optimal signal in this
task, which is the only question the gate asks. The pool is the only item base on
which the rate carries that meaning. Report the rate on T2's frozen set as well,
clearly labelled sampler-dependent, so the two are never confused.

Compute TWO pooled rates over the pool, both of them, and report both:

  grid rate       |D(8)| / N            diverges at or below the grid endpoint
  existence rate  |D(infinity)| / N     diverges at ANY finite budget, i.e. the
                                        fraction of pool items with beta_c < infinity

THE KILL DECISION IS ON THE EXISTENCE RATE, not the grid rate. beta_c = infinity
means the adversary never reorders that item at any budget and the effect genuinely
does not exist there. A finite beta_c > 8 means the effect DOES exist and the
reporting grid is merely too short to show it. Gating on |D(8)| conflates the two
and would kill a real effect whose beta_c distribution happens to sit above 8. The
grid endpoint of 8 is a reporting convention completed by spec section 8.2 and
flagged there as awaiting author confirmation; it does not get to decide whether
Arms B and C run.

Also compute the per-tile rate r_t (manmade |O|=3, moves |O|=3, hold |O|=4,
size |O|=6), ordered by |O|, at BOTH beta = 8 and beta = infinity, and apply spec
section 7.2's gate disambiguation protocol. Spec 7.2 anchors its rates at beta = 8;
the preregistration's gate decides at beta = infinity, so apply the criteria to the
beta = infinity rates and report the beta = 8 rates alongside for comparison against
the spec. This anchor mismatch is a known open item (preregistration Appendix A.2,
item 7) awaiting a spec amendment. Do not resolve it by editing the spec.

Then take exactly one of three outcomes, per preregistration section 7.1:

  1. Existence rate < 0.10. KILL. The adversary effect is absent in this signal
     space. STOP, write up the negative result, and report to the author that Arms
     B and C must not run.
  2. Both rates >= 0.10. PASS. Proceed to Step 2 on the grid as specified.
  3. Existence rate >= 0.10, grid rate < 0.10. The effect is real and the grid is
     too narrow. NOT a kill, and NOT a reason to lower the threshold. Extend the
     divergence curve by appending 16, 32, 64 in order, stopping at the first point
     where |D(beta)| / N >= 0.10; report the rate at every point reached and which
     point stopped the extension. The extension applies to the divergence curve
     only; every other figure and table keeps the original grid. Hard ceiling at
     beta = 64, on interpretability rather than numerics: if the grid rate is still
     below 0.10 there, report that the effect exists but is reachable only at
     persuasion budgets with no plausible interpretation, and carry that as a stated
     limitation rather than as a kill.

The per-tile clean-negative reading is available only when the pooled existence rate
has itself failed. A per-tile pattern does not overturn a passing existence rate.

Emit a machine-readable gate record carrying: the pooled existence rate, the pooled
grid rate, N, the four per-tile rates at each of beta = 8 and beta = infinity, the
sampler-dependent rate on T2's frozen set, the spec version, the disambiguation
verdict, which of the three outcomes obtained, and, for outcome 3, the extended grid
points and where the extension stopped. T7 and T8 confirm the gate by reading that
record; absence of a recorded failure is not a pass.

Do not proceed past this step without an explicit pass or an explicit outcome 3.

STEP 2. Characterize beta_c: distribution over finite values, mass at infinity,
and growth of |D(beta)| in beta. Report the correlation with fit_cost against the
preregistered threshold.

STEP 3. The price of robustness. For each item in the divergence set, compute
L(h*|o*_0) - L(h*|o*_infinity): how much posterior mass on the truth the Scientist
gives up to buy margin. Report the distribution. This number is the paper's answer to
"how much does the adversary actually cost."

STEP 4. What predicts a low beta_c? Regress or otherwise relate beta_c to item
properties available in the manifest. This is exploratory unless preregistered; label
it as such.

STEP 5. Figures. At minimum: |D(beta)| versus beta; the beta_c distribution with the
infinity mass shown honestly rather than dropped; the price-of-robustness
distribution.

ANTI-REQUIREMENTS
- Do not run any language model in this task.
- Do not drop the beta_c = infinity items from plots because they are inconvenient to
  display. Show the mass.
- Every number that will appear in the paper must be emitted by a script into a
  results file, not read off a plot.

DELIVERABLE
Results file, figures, and a plain-language statement of whether the kill gate
passed, with the number.

---

## Note added 2026-09-09 by the T2 session, after K2 fired

Three items. The first is a finding for this task to confirm. The second and third
are corrections to premises this brief states above, which stopped being true when
K2 fired. **None of them licenses a sampling decision, and none was allowed to
motivate one in the session that wrote them.**

### 1. Arm A finding to confirm: the three optima coincide together

Measured over the full 200,000-candidate pool by `src/k2_gate.py`
(`reports/T2_k2_gate.md`), and reproduced on P1's frozen 1,000-item set by
`src/frozen_set_beta_c.py` (`reports/T2_frozen_set_counts.md`):

| conditioned on | N | share adversary-robust (`beta_c = infinity`) |
|---|---:|---:|
| `fit_cost = 0` (non-conflict) | 162,694 | 98.29% |
| `fit_cost > 0` (conflict) | 37,306 | 9.71% |

On the frozen set the same relation reads 0.0160 against 0.9040 in the other
direction: of its 460 finite-`beta_c` items, 452 are conflict items.

**Why this is substantive rather than a nuisance.** The two conditions are different
statements about different pairs of optima:

- `fit_cost = 0` says the **salience** optimum and the **Bayes** optimum coincide,
  `o_fit = o_bayes`.
- `beta_c = infinity` says the **Bayes** optimum and the **margin** optimum coincide,
  `o*_0 = argmax_o margin(o)`.

They share one term and are otherwise unrelated: neither implies the other, and a
signal space in which they were independent would show, in both rows, a robust rate
near the pooled marginal of 0.8177 (the complement of the 0.1823 existence rate).
Instead the rows differ by 88.6 points. The three optima
coincide together far more often than chance, and the adversary effect concentrates
almost entirely inside P1's conflict set: outside it, the optimal signal is already
margin-maximizing and no persuasion budget moves it.

**What T6 does with this.** Confirm it on Arm A's own item base and report it with
the `beta_c` characterization in Step 2. Step 4 is the natural home for the mechanism
question, and remains exploratory there, as spec v3.1 section 7.2 already assigns it.
Do not treat the coincidence as established because it is written here: it was
measured as a byproduct of a gate computation, on a pool, by a session whose brief
was item design.

### 2. Correction: there is no T2 stratified item set

Step 1's rationale above argues the gate must run on the pool because "T2 draws its
item set by stratifying ON beta_c, with an explicit quota for the beta_c = infinity
control stratum." **T2 drew no such set.** K2 fired
(`rho_Spearman(beta_c, fit_cost) = -0.9036` over the pool, threshold 0.80;
`docs/P2/PREREGISTRATION_v2.2.md` section 1), so the `beta_c`-stratified redraw does
not happen and the pre-specified fallback is reuse of P1's frozen
`items_final.parquet`.

**Step 1's instruction is unchanged and its conclusion is unaffected.** The gate still
runs on the full unstratified 200,000-candidate pool. The stratification argument was
one reason for that; the general reason survives it intact, since a divergence rate
on any selected set is a readout of that set's selection rule. Where this brief says
"T2's frozen set," read "the item set Arm A is analysing," and label its rate
sampler-dependent exactly as instructed. Note for Steps 2 through 5: that set's
finite-`beta_c` rate is 0.4600 against the pool's 0.1823, and the gap is fully
accounted for by P1's 50% conflict quota, which was fixed before `beta_c` existed as
a quantity.

### 3. Step 2's `fit_cost` correlation is already computed, and it is K2

Step 2 asks for "the correlation with `fit_cost` against the preregistered
threshold." That is K2, and T2 computed it: `-0.9036`, fires. T6 recomputes it as a
reproduction check, the same standing v2.1 section 1.3 gives T6's K1 run, and a
discrepancy between the two runs is a bug in one of them. It is not a fresh gate and
a second computation does not reopen the outcome.

---

## Patch to the Inputs section, 2026-09-09, by the T6 session, before Step 1 ran

**Reason.** The Inputs section named "frozen item set and manifest from T2." K2
fired, so T2 designed no strata, drew no sample and wrote no item file or manifest.
Every downstream reference in this brief to a P2 item set was therefore stale, and
a session reading the brief alone would have had nothing to point Steps 2 through 5
at.

**How it was resolved.** By reading, not by choosing. The question put to this
session was whether any preregistration version specifies Arm A's base for Steps 2
through 5, with instructions to report a gap and stop if none does. One does:
`PREREGISTRATION_v2.1.md` section 1.3, quoted in the patched Inputs above. It fixes
the frozen item set, and `v2.2` section 1.1 fixes what "T2's frozen item set"
resolves to. No new decision was taken here and none was needed.

**Recorded because it runs against the reading that prompted the check.** The
session was handed the argument that Arm A's claims are about the signal space
rather than about a sample, which argues for the 200,000-candidate pool, and that
`data/pool_beta_c.parquet` already holds the table. That argument is not what the
record says, and the record wins. Where a pool figure is cheap because Step 1
computes it anyway, it is reported beside the frozen-set figure and labelled, so
the two bases are never confused and a later reader can see what the pool-based
version would have looked like. The primary is the frozen set in every case.
