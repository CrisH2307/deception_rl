# T3: prompt conditions F0 / F1 / F2

Generated by `python3 src/framings.py`. Spec `v3.1`, `docs/spec/adversary-game-v1.md`. Deterministic, no seed: the permutations are read from P1's frozen renderings rather than redrawn.


## 1. The three framings

`F0` is P1's Format V `base` rendering, produced by calling `render_items.render` in P1's tree. `F1` and `F2` are that string with one constant block spliced into the `{extra}` slot, the position D64's condition-5 insert occupies, immediately before `Which do you send?`.


**F0** (no block; identical to P1)


**F1** 

```
A third person also sees both lists and already knows the pair. After you send
your description, that person will speak to the other person, and wants them
to name a different pair. They know the pair, and the other person still
does not know what it is.
```


**F2** 

```
A third person also sees both lists and already knows the pair. After you send
your description, that person will speak to the other person, and wants them
to name a different pair. They will argue for whichever wrong pair your
description makes look most likely.
```


## 2. `F0` against Paper 1, byte for byte

Regenerated all **2,000** Format V `base` renderings in `items_rendered.parquet` and compared to the frozen text.

```
renderings compared   2,000
byte-level mismatches 0
```

**Zero content drift.** `F0` is Paper 1's prompt, not a reproduction of it.


And the framings are additive: removing the constant block from every `F1` and `F2` prompt returns the `F0` prompt exactly.

```
F1/F2 prompts round-tripped   4,000
failures to recover F0        0
```


## 3. Length parity, `F1` against `F2`

Sentences 1 and 2 are shared verbatim. Only sentence 3 differs.

| segment | chars | words | in |
|---|---:|---:|---|
| shared, sentences 1-2 | 181 | 33 | both |
| `F1` sentence 3 (neutral filler) | 72 | 15 | F1 only |
| `F2` sentence 3 (mechanism) | 81 | 13 | F2 only |

**Differing-sentence delta: `F2` minus `F1` = -2 words, +9 characters.**


Whole-block and whole-prompt deltas, which are what a model sees:

| block | chars | words |
|---|---:|---:|
| `F1` block | 254 | 48 |
| `F2` block | 263 | 46 |

| tile | \|O\| | F0 median chars | F1 | F2 | F2 - F1 |
|---|---:|---:|---:|---:|---:|
| `manmade` | 3 | 622 | 878 | 887 | +9 |
| `moves` | 3 | 602 | 858 | 867 | +9 |
| `hold` | 4 | 633 | 889 | 898 | +9 |
| `size` | 6 | 767 | 1023 | 1032 | +9 |

The `F2` block is 3.4% longer than `F1`'s, and 1.02% of a median `F1` prompt. Preregistration section 4.5 cell 4 takes this number as the input to its length-artifact diagnosis; it is small enough that a length explanation for an `F1`-only effect would have to survive the `TV(m, F1)` check in the same paragraph, not rest on the word count.


One register difference is stated rather than engineered away: `F2`'s sentence 3 contains the word `wrong` and `F1`'s does not. It cannot be removed, because naming the wrong-answer set is what the mechanism statement is. The shared sentence 2, `wants them to name a different pair`, carries the same content in both framings.


## 4. Leak check

The framing must be computable from the task setup alone: no reference to `h*` beyond what P1's prompt already states, none to the margin, and none to which signal to send. Implemented in `leak_check` and re-run by `tests/test_framings.py`.

| check | violations | result |
|---|---:|---|
| T3 banned words (margin / maximize / optimal / Bayesian) | 0 | **PASS** |
| P1 BANNED (correct / best / optimal / right answer) | 0 | **PASS** |
| P1 GAME_TERMS (19 patterns) | 0 | **PASS** |
| P1 TILE_TERMS (size / manmade / moves / hold) | 0 | **PASS** |
| no option string appears in any block | 0 | **PASS** |
| no pool concept name appears in any block | 0 | **PASS** |
| block is a single paragraph, no blank line | 0 | **PASS** |
| every line wraps at or under 79 characters | 0 | **PASS** |
| F1 and F2 share sentences 1 and 2 verbatim | 0 | **PASS** |

Plus the structural check in section 2: **0** of 4,000 `F1` and `F2` prompts fail to return `F0` when the constant block is removed. That is the check that actually rules out per-item framing content, because a block carrying anything item-specific could not be a module constant and would fail the removal on some item.


## 5. Adversarial review cases

Three per framing. Each is a wording a careless author would write, the quantity it resolves to, and a real item on which that quantity is named. The adopted wording resolves to nothing on any of them: it is a module constant and section 2 shows it is byte-identical across all 2,000 renderings.


**F0, item 192218.** Careless wording:

> Send the description that fits your pair most closely.

Resolves to `o_fit` = `about the size of a chicken egg` on this item. Names a signal outright. On a conflict item `o_fit` is not P1's oracle, so the sentence resolves to a specific option and to the one P1 measures models for over-choosing.

On this item `o_fit` / `o_bayes` / `o*_infinity` are `a chicken egg` / `a grain of sand` / `a chicken egg`, so the careless wording selects among them rather than describing the game.

Adopted `F0`: no block. P1's prompt is unchanged, and section 2 shows it is byte-identical to the frozen rendering for this item.


**F0, item 86066.** Careless wording:

> Send the description that makes your pair stand out furthest from every other pairing.

Resolves to `o_inf` = `about the size of a chicken egg` on this item. The margin rule in words, which T3 item 3 forbids and which under `F0` has no adversary to justify it. It would hand `F0` the `beta -> infinity` answer.

On this item `o_fit` / `o_bayes` / `o*_infinity` are `a chicken egg` / `a grain of sand` / `a chicken egg`, so the careless wording selects among them rather than describing the game.

Adopted `F0`: no block. P1's prompt is unchanged, and section 2 shows it is byte-identical to the frozen rendering for this item.


**F0, item 28004.** Careless wording:

> Send the description the other person is most likely to read as your pair.

Resolves to `o_bayes` = `about the size of a taxi` on this item. States the `F0` answer directly: `o_bayes` is `argmax_o L(h*|o)`.

On this item `o_fit` / `o_bayes` / `o*_infinity` are `a washing machine` / `a taxi` / `a washing machine`, so the careless wording selects among them rather than describing the game.

Adopted `F0`: no block. P1's prompt is unchanged, and section 2 shows it is byte-identical to the frozen rendering for this item.


**F1, item 133212.** Careless wording:

> A third person also sees both lists and already knows the pair, and will argue for whichever other pairing your description fits next best.

Resolves to `d_star` = `slug + oil` on this item. Withholding the mechanism is the entire purpose of `F1`. This states R1 and makes `F1` a second copy of `F2`, destroying the load-bearing contrast.

On this item `o_fit` / `o_bayes` / `o*_infinity` are `a chicken egg` / `a grain of sand` / `a chicken egg`, so the careless wording selects among them rather than describing the game.

Adopted `F1`: the block `sha256:2c0806fcdcf9`, the same string on this item as on every other one.


**F1, item 150490.** Careless wording:

> A third person also sees both lists and already knows the pair is the two objects you were told, and will name something else.

Resolves to `h_star` = `mouth + ear` on this item. Re-states `h*` inside the framing. The block would have to be item-specific and would fail section 2's removal check.

On this item `o_fit` / `o_bayes` / `o*_infinity` are `a chicken egg` / `a grain of sand` / `a chicken egg`, so the careless wording selects among them rather than describing the game.

Adopted `F1`: the block `sha256:2c0806fcdcf9`, the same string on this item as on every other one.


**F1, item 1320.** Careless wording:

> A third person will argue against you, so leave as much room as you can between your pair and the nearest rival.

Resolves to `o_inf` = `about the size of a washing machine` on this item. An instruction about the margin, therefore about which signal to send, smuggled into the framing that is supposed to say only that an adversary exists.

On this item `o_fit` / `o_bayes` / `o*_infinity` are `a washing machine` / `a taxi` / `a washing machine`, so the careless wording selects among them rather than describing the game.

Adopted `F1`: the block `sha256:2c0806fcdcf9`, the same string on this item as on every other one.


**F2, item 5293.** Careless wording:

> They will argue for whichever wrong pair your description makes look most likely, so send the description whose lead over the next pairing is largest.

Resolves to `o_inf` = `about the size of a washing machine` on this item. Sentence 3 as adopted is fine; the trailing clause is not. It turns a description of the adversary into an instruction naming `o*_infinity`.

On this item `o_fit` / `o_bayes` / `o*_infinity` are `a washing machine` / `a taxi` / `a washing machine`, so the careless wording selects among them rather than describing the game.

Adopted `F2`: the block `sha256:ef4495fefd70`, the same string on this item as on every other one.


**F2, item 174894.** Careless wording:

> They will argue for the pairing that comes second under your description, which on this item is the one you can see coming.

Resolves to `d_star` = `kayak + quad` on this item. Item-specific by construction. A block that names the runner-up cannot be constant, and section 2's round trip fails.

On this item `o_fit` / `o_bayes` / `o*_infinity` are `a washing machine` / `a taxi` / `a washing machine`, so the careless wording selects among them rather than describing the game.

Adopted `F2`: the block `sha256:ef4495fefd70`, the same string on this item as on every other one.


**F2, item 8388.** Careless wording:

> They will argue for whichever wrong pair looks most likely, so avoid descriptions under which two pairings look alike.

Resolves to `o_inf` = `about the size of a chicken egg` on this item. The margin rule restated as advice. `F2` may describe what the adversary does; it may not tell the Scientist what to do about it.

On this item `o_fit` / `o_bayes` / `o*_infinity` are `a chicken egg` / `a grain of sand` / `a chicken egg`, so the careless wording selects among them rather than describing the game.

Adopted `F2`: the block `sha256:ef4495fefd70`, the same string on this item as on every other one.


**Summary of what each careless wording would have named:**

| framing | item | quantity | option or pairing it names | the item's `o_fit` / `o_bayes` / `o*_inf` |
|---|---:|---|---|---|
| F0 | 192218 | `o_fit` | `about the size of a chicken egg` | `a chicken egg` / `a grain of sand` |
| F0 | 86066 | `o_inf` | `about the size of a chicken egg` | `a chicken egg` / `a grain of sand` |
| F0 | 28004 | `o_bayes` | `about the size of a taxi` | `a washing machine` / `a taxi` |
| F1 | 133212 | `d_star` | `slug + oil` | `a chicken egg` / `a grain of sand` |
| F1 | 150490 | `h_star` | `mouth + ear` | `a chicken egg` / `a grain of sand` |
| F1 | 1320 | `o_inf` | `about the size of a washing machine` | `a washing machine` / `a taxi` |
| F2 | 5293 | `o_inf` | `about the size of a washing machine` | `a washing machine` / `a taxi` |
| F2 | 174894 | `d_star` | `kayak + quad` | `a washing machine` / `a taxi` |
| F2 | 8388 | `o_inf` | `about the size of a chicken egg` | `a chicken egg` / `a grain of sand` |

The last column is abbreviated: every `size` option reads `about the size of X` and only `X` is shown. Where it lists more than one value the three oracles disagree on that item, which is why a careless wording naming any of them would be naming a specific and different option.


The adopted `F1` and `F2` blocks name none of these. They contain no option string, no pool concept name and no tile term (section 4), and they are one constant string across every item (section 2). `F2`'s sentence 3 states the adversary's rule, `d*(o) = argmax_{h != h*} L(h|o)` from spec section 4, which is a property of the game and not of the item: it is the same sentence whatever `h*`, `o` and `d*` turn out to be.


## 6. Response format

Unchanged across framings. Every prompt ends with the same `'\n\nWhich do you send?'` and the option menu is P1's, so the scored strings are the option texts verbatim under Format V and parsing is untouched (preregistration section 7.2). D104's PMI neutral prompt is built from the menu and the question only, and the framing block sits outside it, exactly as D104 holds the condition-5 insert outside it: the normalisation denominator is identical under `F0`, `F1` and `F2`.


No chain-of-thought scaffolding, hint or worked example is added, and no shared byte is changed, so P1's zero-shot forced choice is what all three framings measure.


## 7. Chain of custody

| artifact | sha256 (first 16) |
|---|---|
| `F0` block | n/a (empty) |
| `F1` block | 2c0806fcdcf96e97 |
| `F2` block | ef4495fefd70c913 |
| P1 `BASE` template | 1773dc0be52f9a7f |
| P1 `items_final.parquet` | ec79b071dcfabd60 |
| P1 `items_rendered.parquet` | c9cb7185f0c88986 |

Frozen framings: `data/reference/framings_p2.json`. Test suite: `tests/test_framings.py`.
